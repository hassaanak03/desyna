
  # Build it now (Copy)

  This is a code bundle for Build it now (Copy). The original project is available at https://www.figma.com/design/646jGuCuFROBIMXVMBalJ8/Build-it-now--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  